"use client";
import { Container, Flex, Box } from "@mantine/core";
import { Card } from "@/components/ui";

const AccommodationsWrapper = (props) => {
  console.log("acs props", props);
  return (
    <Container size={"lg"}>
      <div>AccommodationsWrapper</div>
    </Container>
  );
};

export default AccommodationsWrapper;
