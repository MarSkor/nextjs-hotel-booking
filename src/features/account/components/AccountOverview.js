import { Container, Box, Text, Title } from "@mantine/core";

const AccountOverview = () => {
  return (
    <Container fluid className="accountOverview__container">
      AccountOverview
    </Container>
  );
};

export default AccountOverview;
