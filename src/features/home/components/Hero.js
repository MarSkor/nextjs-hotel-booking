import { Container, Box, Flex, Text, Title } from "@mantine/core";

const Hero = () => {
  return (
    <section>
      <Container size="lg">
        <Title className="hero-title">Hello world</Title>
      </Container>
    </section>
  );
};

export default Hero;
