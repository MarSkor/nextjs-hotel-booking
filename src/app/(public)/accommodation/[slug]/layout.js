import React from "react";

const AccommodationDetailsLayout = ({ children }) => {
  return <section className="ac-details__wrapper">{children}</section>;
};

export default AccommodationDetailsLayout;
